// AI Features have been removed.

export const solveMathProblem = async (problem: string): Promise<string> => {
    return "AI features are disabled.";
};

export const generateSticker = async (prompt: string): Promise<string | null> => {
    return null;
};

export const expandNote = async (text: string): Promise<string> => {
    return "";
}